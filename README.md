# Threshold

A multi-agent simulation exploring how a diverse population responds to shared events, using tabular Q-learning so each agent's behavior evolves through experience rather than fixed rules.
